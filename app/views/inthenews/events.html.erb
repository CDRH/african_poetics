<h1 class="sectiontitle"><%= t "inthenews.title" %></h1>

<%= render "inthenews/top_include" %>

<h2>Events</h2>

<div class="row">
  <div class="col-md-5">
    
    <label for="exampleInput">Search Events (coming soon)</label>
    <div class="input-group">
      <input type="text" class="form-control" placeholder="Search for...">
      <span class="input-group-btn">
        <button class="btn btn-default" type="button">Go!</button>
      </span>
    </div><!-- /input-group -->
    <%= link_to "Browse All Events", inthenews_search_events_path %>

<!--     <ul class="timeslider">
      <li>1864</li>
      <li>1920</li>
      <li>1900</li>
      <li>1940</li>
      <li>1960</li>
      <li>1980</li>
      <li>2000</li>
    </ul>
 -->
    <p>(Coming soon) Search by decade by sliding the timeline to see the distribution of the Poets who had events in the news during that time period.</p>

  </div>

  <div class="col-md-5">
    <%= render "partials/map_africa" %>

    <p>(Coming soon) Select a country to see the distribution of the news about these events in the country.</p>

  </div>

  <div class="col-md-2 inthenews_sidebar">
    <h3>Events</h3>

    <ul>
      <% @event_types.each do |label, count| %>
        <% l = "#{label} (#{count})" %>
        <% path = inthenews_search_events_path(event_type: label) %>
        <li><%= link_to l, path %></li>
      <% end %>
    </ul>
  </div>
</div>
