<h1 class="sectiontitle"><%= t "inthenews.title" %></h1>

<h2>Works Search</h2>

<%= will_paginate @items %>
<%= pluralize @items.count, "Work" %>

<ul>
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, inthenews_work_path(item) %>
    </li>
  <% end %>
</ul>

<%= will_paginate @items %>
