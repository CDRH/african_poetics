<%= render_overridable("items", "index_header") %>
<p>Search the collection of critical commentaries and analysis of articles on African poetry published in the London Times, Sunday Times, Financial Times, the Times Literary Supplement 1865–1985. Authored by graduate students from the University of Nebraska-Lincoln.</p>
<div class="search_form">
  <%= render_overridable("items", "search_box") %>

  <div class="col-sm-3">
    <div class="clearfix">
      <h2 class="pull-left">
        <%= t "search.filters.filter_by", default: "Filter by" %>:
      </h2>
      <%= render_overridable("items", "clear_facets") %>
    </div>
    <%= render_overridable("items", "date_limit") %>
    <%= render_overridable("items", "facets", facets: @res.facets) %>
  </div> <%# col-md-3 %>

  <div class="col-sm-9">
    <%= render_overridable("items", "summary_boxes") %>
    <%= render_overridable("items", "sort_and_page_ui") %>

    <%# search results section %>
    <%= render_overridable("items", "search_res_count") %>
    <%= render_overridable("items", "search_res_items") %>

    <%= render_overridable("items", "sort_and_page_ui") %>
  </div>
</div>
